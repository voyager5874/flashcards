import { ReactElement } from 'react';

import styles from 'features/Flashcard/Flashcard.module.scss';

export const Flashcard = (): ReactElement => {
  const someFunc = () => {};
  return <div className={styles.wrapper}>PacksList</div>;
};
